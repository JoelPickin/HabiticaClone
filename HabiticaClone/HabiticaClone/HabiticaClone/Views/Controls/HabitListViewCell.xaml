<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HabiticaClone.Views.Controls.HabitListViewCell">
    <ViewCell.View>
        <Grid ColumnSpacing="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Text="+" BackgroundColor="#FFB347" VerticalOptions="FillAndExpand" CornerRadius="0" FontSize="Large"/>
            <StackLayout Grid.Column="1" Padding="5" BackgroundColor="White">
                <Label Text="{Binding TaskTitle}" TextColor="Black" HorizontalOptions="Start" FontSize="Subtitle"/>
                <Label Text="{Binding Notes}" TextColor="LightGray" FontSize="Small"/>
            </StackLayout>
            <Button Grid.Column="2" Text="-" BackgroundColor="#FFB347" VerticalOptions="FillAndExpand" TextColor="White" CornerRadius="0" FontSize="Large"/>
        </Grid>
    </ViewCell.View>
</ViewCell>